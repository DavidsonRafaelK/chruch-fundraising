/**
 * This app is single-tenant: every signed-in user shares one workspace.
 * Used as the Liveblocks room prefix. Swap for a real tenant id if the app
 * ever becomes multi-tenant.
 */
export const WORKSPACE_ID = "workspace";
